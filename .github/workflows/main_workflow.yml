name: telegram
on:
  push:
    branches:
      - "telegram"
jobs:
  send_message:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          TELEGRAM_TOKEN: ${{secrets.TELEGRAM_TOKEN}}
          TELEGRAM_CHAT: ${{secrets.TELEGRAM_USR_ID}}